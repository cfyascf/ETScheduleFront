import styled from 'styled-components'

const FooterContainer = styled.div`
    width: 100%;
    height: 1vh;
    display: flex;
`;

const ColoredLine = styled.img `
    width: 100%;
    height: 100%;
    object-fit: cover;
`

export {FooterContainer, ColoredLine};