import styled from 'styled-components'

const NavbarContainer = styled.div `
    width: 100%;
    height: 10vh;

    display: flex;
    flex-direction: column;
`;

const ColoredLine = styled.img `
    width: 100%;
    height: 3vh;
    object-fit: cover;
`

export { NavbarContainer, ColoredLine };

